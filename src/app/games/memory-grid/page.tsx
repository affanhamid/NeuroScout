import React from "react";
import MemoryGrid from "@/components/components/MemoryGrid/MemoryGrid";

const page = () => {
  return (
    <main>
      <MemoryGrid />
    </main>
  );
};

export default page;
